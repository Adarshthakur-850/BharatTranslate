global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'fastapi-backend'
    static_configs:
      - targets: ['backend:8000']
        # Note: You need a /metrics endpoint in FastAPI using something like prometheus-fastapi-instrumentator
